import { getPosts, getRandomPost } from "./src";
import * as Utils from "./utils";
export { Utils, getPosts, getRandomPost };
export declare const getImageChar: (name: string, safe?: boolean, limit?: number) => Promise<Utils.Post[]>;
